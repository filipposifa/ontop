CREATE TEMPORARY TABLE SESSION.viewtable6288 AS select distinct alias1."WLBNAMEPART1" as "WLBNAMEPART1" from 
"PUBLIC"."WELLBORE_EXPLORATION_ALL" alias1

CREATE TEMPORARY TABLE SESSION.viewtable6289 AS select distinct alias1."WLBNAMEPART1" as "WLBNAMEPART1" from 
"PUBLIC"."WELLBORE_EXPLORATION_ALL" alias1 
where 
alias1."WLBNAMEPART1" IS NOT NULL and 
alias1."WLBNAMEPART1" IS NOT NULL

CREATE TEMPORARY TABLE SESSION.viewtable6290 AS select distinct alias2."WLBNAMEPART1" as "WLBNAMEPART1" from 
"PUBLIC"."WELLBORE_SHALLOW_ALL" alias2

CREATE TEMPORARY TABLE SESSION.viewtable6291 AS select distinct alias0."WLBNAMEPART1" as "WLBNAMEPART1" from 
"PUBLIC"."WELLBORE_DEVELOPMENT_ALL" alias0 
where 
alias0."WLBNAMEPART1" IS NOT NULL and 
alias0."WLBNAMEPART1" IS NOT NULL

CREATE TEMPORARY TABLE SESSION.viewtable6292 AS select distinct alias0."WLBNAMEPART1" as "WLBNAMEPART1" from 
"PUBLIC"."WELLBORE_DEVELOPMENT_ALL" alias0

SELECT

   1 AS "qQuestType", NULL AS "qLang", CAST(('http://sws.ifi.uio.no/data/npd-v2/quadrant/' || REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(CAST(var0 AS VARCHAR(500)), ' ', '%20'), '!', '%21'), '''', '%22'), '#', '%23'), '$', '%24'), '&', '%26'), '(', '%28'), ')', '%29'), '*', '%2A'), '+', '%2B'), ',', '%2C'), '/', '%2F'), ':', '%3A'), ';', '%3B'), '=', '%3D'), '?', '%3F'), '@', '%40'), '[', '%5B'), ']', '%5D')) AS VARCHAR(500)) AS "q", 
   7 AS "nQuestType", NULL AS "nLang", CAST(var1 AS VARCHAR(500)) AS "n"
FROM (
 (
SELECT 
   QVIEW1."WLBNAMEPART1" as var0, 
   QVIEW1."WLBNAMEPART1" as var1
 FROM 
SESSION.VIEWTABLE6289 QVIEW1,
SESSION.VIEWTABLE6290 QVIEW2
WHERE 
(QVIEW1."WLBNAMEPART1" = QVIEW2."WLBNAMEPART1"))
 
UNION
(
SELECT 
   QVIEW1."WLBNAMEPART1" as var0, 
   QVIEW1."WLBNAMEPART1" as var1
 FROM 
SESSION.VIEWTABLE6291 QVIEW1)
 
UNION
(
SELECT 
   QVIEW1."WLBNAMEPART1" as var0, 
   QVIEW1."WLBNAMEPART1" as var1
 FROM 
SESSION.VIEWTABLE6290 QVIEW1,
SESSION.VIEWTABLE6291 QVIEW2
WHERE 
(QVIEW1."WLBNAMEPART1" = QVIEW2."WLBNAMEPART1"))
 
UNION
(
SELECT 
   QVIEW1."WLBNAMEPART1" as var0, 
   QVIEW1."WLBNAMEPART1" as var1
 FROM 
SESSION.VIEWTABLE6289 QVIEW1,
SESSION.VIEWTABLE6292 QVIEW2
WHERE 
(QVIEW1."WLBNAMEPART1" = QVIEW2."WLBNAMEPART1"))
 
UNION
(
SELECT 
   QVIEW1."WLBNAMEPART1" as var0, 
   QVIEW1."WLBNAMEPART1" as var1
 FROM 
SESSION.VIEWTABLE6289 QVIEW1)
 
UNION
(
SELECT 
   QVIEW1."WLBNAMEPART1" as var0, 
   QVIEW1."WLBNAMEPART1" as var1
 FROM 
SESSION.VIEWTABLE6288 QVIEW1,
SESSION.VIEWTABLE6291 QVIEW2
WHERE 
(QVIEW1."WLBNAMEPART1" = QVIEW2."WLBNAMEPART1")
 ) 
 
 ) SUBQALIAS